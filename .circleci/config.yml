version: 2.1
executors:
  my_executor:
    docker:
      - image: mikio2225/td-toolbelt:latest

jobs:
  digdag:
    executor: my_executor
    steps:
      - run:
          name: Push project
          command: ./push.sh

workflows:
  version: 2
  build-n-deploy:
    jobs:
      - digdag